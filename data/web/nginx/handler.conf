# Use this file to specify how to handle PHP-files. We've provided two default
# configurations, one for PHP-FPM (default) and one for HHVM (experimental).
# The contents of these files should be sufficient for most needs. If you find
# a need to write your own handler, please let us know!

# Note: changes you make to this file will be automatically loaded in nginx when
# you save the file. If you made a mistake, just revert the change and save the
# file again.

# See http://support.hypernode.com/using-hhvm.html for more information


# Scenario 1: you want to run your entire site in HHVM, no exceptions
#
# uncomment these lines:

#location ~ \.php$ {
#    echo_exec @hhvm;
#}

# =====================================================================

# Scenario 2: you want to run your entire site in HHVM, while the API
# should be served by PHP-FPM
#
# uncomment these lines:

#location /api/soap {
#    rewrite / /index.php break;
#    echo_exec @phpfpm;
#}
#location ^~ /index.php/api/soap {
#    rewrite / /index.php break;
#    echo_exec @phpfpm;
#}
#location ~ \.php$ {
#    echo_exec @hhvm;
#}

# =====================================================================

# Scenario 3: you want to run your entire site in PHP-FPM, while testing
# some URLs with HHVM

# uncomment these lines:

#location /apparel/hoodies.html {
#    rewrite / /index.php break;
#    echo_exec @hhvm;
#}
#location ^~ /index.php/apparel/hoodies.html {
#    rewrite / /index.php break;
#    echo_exec @hhvm;
#}

# =====================================================================

# Scenario 4: you want to use PHP-FPM as default, but want all PHP-files
# in a subdirectory to be executed by HHVM

# uncomment these lines:

#location /blog/ {
#    location ~ \.php$ {
#        echo_exec @hhvm;
#    }
#}

# =====================================================================

# Scenario 5: the other way around: HHVM should be default, while having
# PHP-FPM execute a script and a subdirectory

# uncomment these lines:
#location ^~ /phpinfo.php {
#    echo_exec @phpfpm;
#}
#location /blog/ {
#    location ~ \.php$ {
#        echo_exec @phpfpm;
#    }
#}
#location ~ \.php$ {
#    echo_exec @hhvm;
#}

# =====================================================================
